<h2 class="mt-5 text-center">Here's your collection <%= user.email %>!</h2>

<ul class="nav nav-tabs d-flex justify-content-center mt-5" id="profile-tab" role="tablist">
    <li class="nav-item" role="presentation">
        <button class="nav-link active" id="mounts-tab" data-bs-toggle="tab" data-bs-target="#mounts-tab-pane" type="button" role="tab" aria-controls="mounts-tab-pane" aria-selected="true">Mounts</button>
    </li>

    <li class="nav-item" role="presentation">
        <button class="nav-link" id="minions-tab" data-bs-toggle="tab" data-bs-target="#minions-tab-pane" type="button" role="tab" aria-controls="minions-tab-pane" aria-selected="false">Minions</button>
    </li>

    <li class="nav-item" role="presentation">
        <button class="nav-link" id="emotes-tab" data-bs-toggle="tab" data-bs-target="#emotes-tab-pane" type="button" role="tab" aria-controls="emotes-tab-pane" aria-selected="false">Emotes</button>
    </li>

    <li class="nav-item" role="presentation">
        <button class="nav-link" id="bardings-tab" data-bs-toggle="tab" data-bs-target="#bardings-tab-pane" type="button" role="tab" aria-controls="bardings-tab-pane" aria-selected="false">Bardings</button>
    </li>
</ul>

<div class="tab-content container text-center" id="profile-tab-content">
    <!-- Mounts Tab -->
    <div class="my-3 tab-pane fade show active" id="mounts-tab-pane" role="tabpanel" aria-labelledby="mounts-tab" tabindex="0">
        <% if (mounts.length <= 0) { %>
            <p class="my-3">No mounts saved 😞 Try searching for some to track!</p>
        <% } %>

        <% mounts.forEach(mount => { %>
            <ul class="list-group list-group-horizontal my-3">
                <li class="list-group-item d-flex align-items-center">
                    <form class="btn-group" method="POST" action="/mounts/<%= mount.id %>/?_method=PUT">
                        <% if (mount.obtained) { %>
                                <input class="btn-check" id="<%= mount.id %>-obtained-mount" type="radio" value="t" name="obtained" checked />
                                <label class="btn btn-outline-success" for="<%= mount.id %>-obtained-mount">Have</label>

                                <input class="btn-check" id="<%= mount.id %>-unobtained-mount" type="radio" value="f" name="obtained" />
                                <label class="btn btn-outline-warning lh-1" for="<%= mount.id %>-unobtained-mount">Don't have</label>    
                        <% } else { %>
                                <input class="btn-check" id="<%= mount.id %>-obtained-mount" type="radio" value="t" name="obtained" />
                                <label class="btn btn-outline-success" for="<%= mount.id %>-obtained-mount">Have</label>

                                <input class="btn-check" id="<%= mount.id %>-unobtained-mount" type="radio" value="f" name="obtained" checked />
                                <label class="btn btn-outline-warning lh-1" for="<%= mount.id %>-unobtained-mount">Don't have</label>
                        <% } %>
                    </form>
                </li>

                <a class="list-group-item list-group-item-action d-flex justify-content-start align-items-center" href="/mounts/<%= mount.apiId %>">
                    <img class="me-2" src="<%= mount.icon %>" />
                    <h5 class="d-inline mx-2"><%= mount.name %></h5>
                </a>

                <form class="list-group-item list-group-item-danger d-flex align-items-center" method="POST" action="/mounts/<%= mount.id %>/?_method=DELETE">
                    <button class="btn btn-outline-danger" type="submit">Remove</button>
                </form>
            </ul>
        <% }) %>
    </div>

    <!-- Minions Tab -->
    <div class="tab-pane fade" id="minions-tab-pane" role="tabpanel" aria-labelledby="minions-tab" tabindex="0">
        <% if (minions.length <= 0) { %>
            <p class="my-3">No minions saved 😞 Try searching for some to track!</p>
        <% } %>

        <% minions.forEach(minion => { %>
            <ul class="list-group list-group-horizontal my-3">
                <li class="list-group-item d-flex align-items-center">
                    <form class="btn-group" method="POST" action="/minions/<%= minion.id %>/?_method=PUT">
                        <% if (minion.obtained) { %>
                            <input class="btn-check" id="<%= minion.id %>-obtained-minion" type="radio" value="t" name="obtained" checked />
                            <label class="btn btn-outline-success" for="<%= minion.id %>-obtained-minion">Have</label>

                            <input class="btn-check" id="<%= minion.id %>-unobtained-minion" type="radio" value="f" name="obtained" />
                            <label class="btn btn-outline-warning lh-1" for="<%= minion.id %>-unobtained-minion">Don't have</label>    
                        <% } else { %>
                            <input class="btn-check" id="<%= minion.id %>-obtained-minion" type="radio" value="t" name="obtained" />
                            <label class="btn btn-outline-success" for="<%= minion.id %>-obtained-minion">Have</label>

                            <input class="btn-check" id="<%= minion.id %>-unobtained-minion" type="radio" value="f" name="obtained" checked />
                            <label class="btn btn-outline-warning lh-1" for="<%= minion.id %>-unobtained-minion">Don't have</label>
                        <% } %>
                    </form>
                </li>

                <a class="list-group-item list-group-item-action d-flex justify-content-start align-items-center" href="/minions/<%= minion.apiId %>">
                    <img class="me-2" src="<%= minion.icon %>" />
                    <h5 class="d-inline mx-2"><%= minion.name %></h5>
                </a>

                <form class="list-group-item list-group-item-danger d-flex align-items-center" method="POST" action="/minions/<%= minion.id %>/?_method=DELETE">
                    <button class="btn btn-outline-danger" type="submit">Remove</button>
                </form>
            </ul>
        <% }) %>
    </div>

    <!-- Emotes Tab -->
    <div class="tab-pane fade" id="emotes-tab-pane" role="tabpanel" aria-labelledby="emotes-tab" tabindex="0">
        <% if (emotes.length <= 0) { %>
            <p class="my-3">No emotes saved 😞 Try searching for some to track!</p>
        <% } %>

        <% emotes.forEach(emote => { %>
            <ul class="list-group list-group-horizontal my-3">
                <li class="list-group-item d-flex align-items-center">
                    <form class="btn-group" method="POST" action="/emotes/<%= emote.id %>/?_method=PUT">
                        <% if (emote.obtained) { %>
                            <input class="btn-check" id="<%= emote.id %>-obtained-emote" type="radio" value="t" name="obtained" checked />
                            <label class="btn btn-outline-success" for="<%= emote.id %>-obtained-emote">Have</label>

                            <input class="btn-check" id="<%= emote.id %>-unobtained-emote" type="radio" value="f" name="obtained" />
                            <label class="btn btn-outline-warning lh-1" for="<%= emote.id %>-unobtained-emote">Don't have</label>    
                        <% } else { %>
                            <input class="btn-check" id="<%= emote.id %>-obtained-emote" type="radio" value="t" name="obtained" />
                            <label class="btn btn-outline-success" for="<%= emote.id %>-obtained-emote">Have</label>

                            <input class="btn-check" id="<%= emote.id %>-unobtained-emote" type="radio" value="f" name="obtained" checked />
                            <label class="btn btn-outline-warning lh-1" for="<%= emote.id %>-unobtained-emote">Don't have</label>
                        <% } %>
                    </form>
                </li>

                <a class="list-group-item list-group-item-action d-flex justify-content-start align-items-center" href="/emotes/<%= emote.apiId %>">
                    <img class="me-2" src="<%= emote.icon %>" />
                    <h5 class="d-inline mx-2"><%= emote.name %></h5>
                </a>

                <form class="list-group-item list-group-item-danger d-flex align-items-center" method="POST" action="/emotes/<%= emote.id %>/?_method=DELETE">
                    <button class="btn btn-outline-danger" type="submit">Remove</button>
                </form>
            </ul>
        <% }) %>
    </div>

    <!-- Bardings Tab -->
    <div class="tab-pane fade" id="bardings-tab-pane" role="tabpanel" aria-labelledby="bardings-tab" tabindex="0">
        <% if (bardings.length <= 0) { %>
            <p class="my-3">No bardings saved 😞 Try searching for some to track!</p>
        <% } %>

        <% bardings.forEach(barding => { %>
            <ul class="list-group list-group-horizontal my-3">
                <li class="list-group-item d-flex align-items-center">
                    <form class="btn-group" method="POST" action="/bardings/<%= barding.id %>/?_method=PUT">
                        <% if (barding.obtained) { %>
                            <input class="btn-check" id="<%= barding.id %>-obtained-barding" type="radio" value="t" name="obtained" checked />
                            <label class="btn btn-outline-success" for="<%= barding.id %>-obtained-barding">Have</label>

                            <input class="btn-check" id="<%= barding.id %>-unobtained-barding" type="radio" value="f" name="obtained" />
                            <label class="btn btn-outline-warning lh-1" for="<%= barding.id %>-unobtained-barding">Don't have</label>    
                        <% } else { %>
                            <input class="btn-check" id="<%= barding.id %>-obtained-barding" type="radio" value="t" name="obtained" />
                            <label class="btn btn-outline-success" for="<%= barding.id %>-obtained-barding">Have</label>

                            <input class="btn-check" id="<%= barding.id %>-unobtained-barding" type="radio" value="f" name="obtained" checked />
                            <label class="btn btn-outline-warning lh-1" for="<%= barding.id %>-unobtained-barding">Don't have</label>
                        <% } %>
                    </form>
                </li>

                <a class="list-group-item list-group-item-action d-flex justify-content-start align-items-center" href="/bardings/<%= barding.apiId %>">
                    <img class="me-2" src="<%= barding.icon %>" />
                    <h5 class="d-inline mx-2"><%= barding.name %></h5>
                </a>

                <form class="list-group-item list-group-item-danger d-flex align-items-center" method="POST" action="/bardings/<%= barding.id %>/?_method=DELETE">
                    <button class="btn btn-outline-danger" type="submit">Remove</button>
                </form>
            </ul>
        <% }) %>
    </div>
</div>